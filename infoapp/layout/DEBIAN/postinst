#!/bin/sh

# Đăng ký với LaunchServices
if [ -x "/var/jb/usr/bin/uicache" ]; then
    /var/jb/usr/bin/uicache -p /var/jb/Applications/DeviceStats.app -r
elif [ -x "/usr/bin/uicache" ]; then
    /usr/bin/uicache -p /Applications/DeviceStats.app -r
fi

exit 0